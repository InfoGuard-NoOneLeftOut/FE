import 'package:flutter/material.dart';

class OptionsScreen extends StatelessWidget { // 더보기 페이지
  const OptionsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(child: Text('더보기 페이지')),
    );
  }
}
